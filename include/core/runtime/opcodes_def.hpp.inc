OPCODE(nop),

OPCODE(new_instance),
OPCODE(new_instance_stack),

OPCODE(invoke_method),

TYPED_OPCODES(push),
TYPED_OPCODES(pop),

TYPED_OPCODES(set_slot),
TYPED_OPCODES(set_class_slot),
TYPED_OPCODES(get_slot),
TYPED_OPCODES(get_class_slot),
